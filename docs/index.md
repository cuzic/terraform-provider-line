---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "line Provider"
description: |-
  Manages LINE Messaging API configuration resources (webhook endpoint, LIFF apps, rich menus) declaratively. Unofficial and unaffiliated with LINE Corporation / LY Corporation.
---

# line Provider

Manages LINE Messaging API configuration resources (webhook endpoint, LIFF apps, rich menus) declaratively. Unofficial and unaffiliated with LINE Corporation / LY Corporation.

## Example Usage

```terraform
terraform {
  required_providers {
    line = {
      source = "cuzic/line"
    }
  }
}

provider "line" {
  # channel_access_token can also be supplied via the LINE_CHANNEL_ACCESS_TOKEN
  # environment variable, which is the recommended way to avoid writing it
  # into configuration or committing it to version control.
  channel_access_token = var.line_channel_access_token
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `channel_access_token` (String, Sensitive) LINE channel access token used to authenticate to the Messaging API and LIFF server API. Can also be set via the LINE_CHANNEL_ACCESS_TOKEN environment variable, which is recommended so the token isn't written into Terraform configuration (it will still be stored in state; see the README's note on secrets).
