---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "line_webhook_endpoint Resource - terraform-provider-line"
subcategory: ""
description: |-
  Manages a channel's Messaging API webhook endpoint URL (PUT/GET /v2/bot/channel/webhook/endpoint). This is a channel-level singleton — there is exactly one per channel access token — so there is no create/delete lifecycle in the usual sense: deleting this resource clears the endpoint URL rather than removing anything server-side.
---

# line_webhook_endpoint (Resource)

Manages a channel's Messaging API webhook endpoint URL (PUT/GET /v2/bot/channel/webhook/endpoint). This is a channel-level singleton — there is exactly one per channel access token — so there is no create/delete lifecycle in the usual sense: deleting this resource clears the endpoint URL rather than removing anything server-side.

## Example Usage

```terraform
resource "line_webhook_endpoint" "this" {
  endpoint = "https://example.com/webhook"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `endpoint` (String) The webhook URL. Must use the https scheme and be at most 500 characters.

### Read-Only

- `active` (Boolean) Whether the LINE Platform will deliver webhook events to this endpoint. This is informational only — the Messaging API exposes no operation in this provider's scope to toggle it, so it always reflects whatever LINE currently reports.
- `id` (String) Fixed identifier for this singleton resource; not a LINE-assigned ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The webhook endpoint is a channel-level singleton, so any non-empty
# string works as the import ID.
terraform import line_webhook_endpoint.this webhook_endpoint
```
